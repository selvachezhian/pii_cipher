# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in pii_cipher.gemspec
gemspec

gem "irb"
gem "rake", "~> 13.0"

gem "rake-compiler"
gem "rake-compiler-dock", "~> 1.5"

gem "rspec", "~> 3.0"

gem "rubocop", "~> 1.21"

# Integration specs run against PostgreSQL (partial search uses jsonb `@>`).
gem "pg", "~> 1.5"
